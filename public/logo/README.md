# Add the school crest here

The supplied crest is stored here as `rusinga-crest.png` and is used by the navbar and footer. Replace that file with a higher-resolution version when available; no code changes are needed.
